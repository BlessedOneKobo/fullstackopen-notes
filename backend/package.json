{
  "name": "notes",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../../part2/notes && npm run build && cp -r dist ../../part3/notes",
    "deploy:full": "npm run build:ui && git add . && git commit -u uibuild && git push",
    "start": "node index.js",
    "dev": "node --env-file=.env --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abel Kalu",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1"
  }
}
